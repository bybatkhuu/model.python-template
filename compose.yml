services:
  model:
    image: bybatkhuu/model.python-template:latest
    build:
      context: .
    restart: unless-stopped
    environment:
      TERM: ${TERM:-xterm}
      TZ: ${TZ:-Asia/Seoul}
      SSH_PORT: ${SSH_PORT:-22}
      JUPYTERLAB_PORT: ${JUPYTERLAB_PORT:-8888}
    env_file:
      - path: .env
        required: false
    ports:
      - "${SSH_PORT:-22}:${SSH_PORT:-22}"
      - "${JUPYTERLAB_PORT:-8888}:${JUPYTERLAB_PORT:-8888}"
    tty: true
